import { RendererPracDirective } from './renderer-prac.directive';

describe('RendererPracDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererPracDirective();
    expect(directive).toBeTruthy();
  });
});
